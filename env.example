# Copy this to .env.local and fill in your values

# Google Places API (Required for business search)
NEXT_PUBLIC_GOOGLE_PLACES_API_KEY=your_google_places_api_key_here

# ALTCHA Configuration (Required for captcha verification)
ALTCHA_SECRET_KEY=your_altcha_secret_key_here

# OSM API Credentials (Required for OSM uploads)
OSM_CLIENT_ID=your_osm_client_id_here
OSM_CLIENT_SECRET=your_osm_client_secret_here
OSM_REFRESH_TOKEN=your_osm_refresh_token_here

# Supabase Configuration
# Option 1: Use direct database connection string (RECOMMENDED)
# Get this from Supabase Dashboard > Settings > Database > Connection string (URI)
# Select "URI" format and copy the connection string
DATABASE_URL=postgresql://postgres.[PROJECT_REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true

# Option 2: Use Supabase variables (alternative - auto-constructs DATABASE_URL)
# SUPABASE_URL=https://your-project-ref.supabase.co
# SUPABASE_KEY=your_supabase_anon_key_here
# SUPABASE_DB_PASSWORD=your_database_password_here
# SUPABASE_REGION=ap-southeast-2  # Optional: your Supabase region (defaults to ap-southeast-2)

# Application URL
NEXT_PUBLIC_APP_URL=http://localhost:3000

# NextAuth Configuration (Required for admin authentication)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your_nextauth_secret_here
# Generate a secret with: openssl rand -base64 32

# Mailjet Configuration (Required for contact form)
MAILJET_API_KEY=your_mailjet_api_key_here
MAILJET_API_SECRET=your_mailjet_api_secret_here
MAILJET_FROM_EMAIL=noreply@bitcoinmerchants.com.au
